# Database root password used by all three Java servers (database.properties).
DB_PASSWORD=aion

# IP the GameServer advertises to clients (ipconfig.xml default).
# LAN players connect to this IP on ports 2106 / 7777 / 10241.
# Set this to the LAN IP of the machine running docker compose.
GAME_IP=127.0.0.1